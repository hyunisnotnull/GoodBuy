<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../include/title.ejs') %>
    <link href="/css/user/mylist.css" rel="stylesheet" type="text/css">

</head>
<body>

    <%- include('../include/header.ejs') %>
    <%- include('../include/nav.ejs') %>
    
    <article>
        <div class="article_wrap">
            <div class="title">
                <h4>MY WISH LIST</h4>
            </div>
            <div class="content">
                <table>
                    <thead>
                        <tr>
                            <th>상품 이미지</th>
                            <th>상품 이름</th>
                            <th>가격</th>
                            <th>등록일</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (products.length === 0) { %>
                            <tr>
                                <td colspan="4">찜한 상품이 없습니다.</td>
                            </tr>
                        <% } else { %>
                            <% products.forEach(product => { %>
                                <tr>
                                    <td><img src="\<%= product.P_OWNER_ID %>\<%= product.P_IMAGE %>" alt="<%= product.P_NAME %>" style="width: 50px; height: auto;" /></td>
                                    <td><%= product.P_NAME %></td>
                                    <td><%= product.P_PRICE.toLocaleString() %> 원</td>
                                    <td><%= product.W_REG_DATE.toLocaleDateString() %></td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>

        </div>
    </article>

    <%- include('../include/footer.ejs') %>
</body>
</html>